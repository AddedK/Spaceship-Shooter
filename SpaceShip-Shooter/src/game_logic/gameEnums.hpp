#ifndef GAME_ENUMS_HPP
#define GAME_ENUMS_HPP
namespace GameLogic {

enum class KeyPress { LEFT, UP, RIGHT, DOWN, ENTER, SPACE, Q };
enum class MoveDirection { LEFT, UP, RIGHT, DOWN };
} // namespace GameLogic

#endif
